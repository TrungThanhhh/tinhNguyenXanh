@{
    ViewData["Title"] = "Đóng góp";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var bankName = "TPBank";
    var accountName = "NguyenTrungThanh";
    var accountNumber = "06345993601";
    var qrImageUrl = "/images/donate-qr.png"; // Đặt ảnh QR vào wwwroot/images
    var noteTemplate = "TNV [Họ tên] [SĐT]";
}
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet" />

<style>
    :root {
        --primary: #16a34a;
        --border: #e5e7eb;
        --text: #0f172a;
        --muted: #64748b;
        --accent: #f0fdf4;
    }

    .donate-wrap {
        padding: 3rem 0;
        min-height: 70vh;
    }

    .card-clean {
        background: #fff;
        border: 1px solid var(--border);
        border-radius: 16px;
        padding: 1.75rem;
        box-shadow: 0 6px 24px rgba(0,0,0,.06);
        display: flex;
        flex-direction: column;
        height: 100%;
    }

    .title {
        font-weight: 800;
        color: var(--text);
        letter-spacing: -0.02em;
    }

    .sub {
        color: var(--muted);
    }

    .info-list {
        list-style: none;
        padding: 0;
        margin: 0;
    }

        .info-list li {
            padding: .6rem 0;
            border-bottom: 1px dashed var(--border);
            display: flex;
            gap: .75rem;
            align-items: center;
        }

            .info-list li:last-child {
                border-bottom: none;
            }

    .qr-box {
        background: linear-gradient(135deg, #fff 0%, var(--accent) 100%);
        border: 1px solid var(--border);
        border-radius: 16px;
        padding: 1.25rem;
        text-align: center;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        height: 100%;
    }

    .qr-img {
        width: 100%;
        max-width: 320px;
        height: auto;
        border-radius: 12px;
        box-shadow: 0 6px 24px rgba(0,0,0,.08);
    }

    .hint {
        font-size: .95rem;
        color: var(--muted);
    }

    .badge-bank {
        display: inline-flex;
        align-items: center;
        gap: .5rem;
        padding: .4rem .75rem;
        border-radius: 999px;
        background: var(--accent);
        color: #065f46;
        border: 1px solid #bbf7d0;
    }

    /* Equal height columns: use flex on row */
    .row-eq > [class*='col-'] {
        display: flex;
    }

    .row-eq .card-clean {
        flex: 1 1 auto;
    }

    /* Small utilities */
    .account-block {
        display: flex;
        gap: .5rem;
        align-items: center;
        flex-wrap: wrap;
    }

    .account-number {
        font-weight: 700;
        font-size: 1.05rem;
        letter-spacing: .4px;
    }

    .btn-copy {
        border-radius: 8px;
        padding: .45rem .7rem;
        font-weight: 600;
    }

    /* Responsive tweaks */
    media (max-width: 767.98px) {
        .qr-img

    {
        max-width: 220px;
    }

    }
</style>

<div class="donate-wrap">
    <div class="container">
        <div class="text-center mb-4">
            <h2 class="title">Đóng góp vì cộng đồng</h2>
            <p class="sub">Quét QR hoặc chuyển khoản theo thông tin bên dưới để ủng hộ quỹ. Ghi rõ nội dung để chúng tôi xác nhận.</p>
        </div>

        <div class="row g-4 row-eq">
            <div class="col-lg-6">
                <div class="card-clean">
                    <div class="d-flex justify-content-between align-items-start mb-3">
                        <div>
                            <h5 class="m-0"><i class="bi bi-bank2 me-2 text-success"></i>Thông tin chuyển khoản</h5>
                            <div class="text-muted small mt-1">Thông tin tài khoản nhận tiền</div>
                        </div>
                        <span class="badge-bank"><i class="bi bi-shield-check"></i> Minh bạch</span>
                    </div>

                    <div class="flex-grow-1">
                        <ul class="info-list mb-3">
                            <li><i class="bi bi-building text-success"></i> <strong style="min-width:120px;display:inline-block">Ngân hàng:</strong> <span>@bankName</span></li>
                            <li><i class="bi bi-person-badge text-success"></i> <strong style="min-width:120px;display:inline-block">Chủ tài khoản:</strong> <span>@accountName</span></li>
                            <li>
                                <i class="bi bi-hash text-success"></i> <strong style="min-width:120px;display:inline-block">Số tài khoản:</strong>
                                <div class="account-block">
                                    <span class="account-number">@accountNumber</span>
                                    <button type="button" class="btn btn-outline-secondary btn-sm btn-copy" data-copy="@accountNumber" title="Sao chép số tài khoản">
                                        <i class="bi bi-clipboard"></i> Sao chép
                                    </button>
                                </div>
                            </li>
                            <li><i class="bi bi-pencil-square text-success"></i> <strong style="min-width:120px;display:inline-block">Nội dung:</strong> <span>@noteTemplate</span></li>
                        </ul>

                        <div class="mb-3">
                            <h6 class="mb-2">Hướng dẫn nhanh</h6>
                            <ol class="small text-muted" style="padding-left:1rem;">
                                <li>Quét mã QR hoặc chuyển khoản theo thông tin trên.</li>
                                <li>Ghi nội dung theo mẫu để chúng tôi đối soát: <strong>@noteTemplate</strong>.</li>
                                @* <li>Gửi ảnh biên lai/chuyển khoản vào email của tổ chức để nhận xác nhận(trungthanh070104@gmail.com).</li> *@
                            </ol>
                        </div>
                    </div>

                    @* <div class="mt-3 d-flex gap-2"> *@
                        @* <a href="https://mail.google.com/mail/u/0/#inbox" class="btn btn-outline-primary btn-sm"><i class="bi bi-envelope"></i> Gửi biên lai qua email</a> *@
                        @* <a href="#" class="btn btn-outline-secondary btn-sm" onclick="window.print(); return false;"><i class="bi bi-printer"></i> In thông tin</a> *@
                    @* </div> *@
                </div>
            </div>

            <div class="col-lg-6">
                <div class="card-clean">
                    <div class="d-flex justify-content-between align-items-start mb-3">
                        <div>
                            <h5 class="m-0"><i class="bi bi-qr-code me-2 text-success"></i>Quét mã QR để ủng hộ nhanh</h5>
                            <div class="text-muted small mt-1">Mở ứng dụng ngân hàng/MoMo/VNPay, chọn Quét QR</div>
                        </div>
                        <span class="badge-bank"><i class="bi bi-qr-code-scan"></i> Nhanh & tiện</span>
                    </div>

                    <div class="flex-grow-1 d-flex align-items-center justify-content-center">
                        <div class="qr-box w-100">
                            <img src="@qrImageUrl" alt="QR Donate" class="qr-img mb-3" />
                            <div class="hint">Quét mã và nhập số tiền, hoặc chọn số tiền có sẵn trong ứng dụng.</div>
                            <div class="mt-3 d-flex gap-2 justify-content-center">
                                <button type="button" class="btn btn-outline-secondary btn-sm" onclick="downloadQR()"><i class="bi bi-download"></i> Tải QR</button>
                                <button type="button" class="btn btn-success btn-sm" onclick="copyAccount()"><i class="bi bi-clipboard-check"></i> Sao chép TK</button>
                            </div>
                        </div>
                    </div>

                    <div class="mt-3 text-center small text-muted">
                        Nếu cần hỗ trợ, liên hệ: <strong>contact@yourorg.org</strong> hoặc Zalo: <strong>0123 456 789</strong>
                    </div>
                </div>
            </div>
        </div>

        <div class="text-center mt-4 sub">
            Cảm ơn sự đóng góp của bạn! Mọi khoản ủng hộ đều được ghi nhận và công khai minh bạch.
        </div>
    </div>
</div>

<script>
    // Sao chép số tài khoản từ nút
    document.querySelectorAll('.btn-copy').forEach(btn => {
        btn.addEventListener('click', function () {
            const value = this.getAttribute('data-copy');
            navigator.clipboard?.writeText(value).then(() => {
                this.innerHTML = '<i class="bi bi-clipboard-check"></i> Đã sao chép';
                setTimeout(() => this.innerHTML = '<i class="bi bi-clipboard"></i> Sao chép', 2000);
            }).catch(() => alert('Không thể sao chép. Vui lòng sao chép thủ công.'));
        });
    });

    // Sao chép tài khoản từ sidebar button
    function copyAccount() {
        const acc = '@accountNumber';
        navigator.clipboard?.writeText(acc).then(() => {
            alert('Số tài khoản đã được sao chép: ' + acc);
        }).catch(() => alert('Không thể sao chép. Vui lòng sao chép thủ công.'));
    }

    // Tải ảnh QR
    function downloadQR() {
        const url = '@qrImageUrl';
        const a = document.createElement('a');
        a.href = url;
        a.download = 'donate-qr.png';
        document.body.appendChild(a);
        a.click();
        a.remove();
    }
</script>
